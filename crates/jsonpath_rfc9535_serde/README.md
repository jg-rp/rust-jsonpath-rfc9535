# An RFC 9535 JSONPath engine using Serde JSON

This crate is mostly experimental. I do not intend to publish it and it does not attempt to replace existing implementations of JSONPath using Serde JSON.
